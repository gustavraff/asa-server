# Logs and result files
*.log
*-last-result.txt
*-result.txt

# Backups and generated data
backups/
config-snapshots/
test-results/
.image-review/
usage-audit/
AI-THREAD-HANDOFF.md

# Server/game binaries and SteamCMD
server/
steamcmd/

# Local config / credentials
server-config.cmd
server-config.cmd.bak

# Temporary state
*.json
.aider*

# The local web manager's source manifests are part of the project.
!web-manager/package.json
!web-manager/package-lock.json
!web-manager/.openai/hosting.json

# ASA settings knowledge base: curated source-of-truth data for
# ASA-AI-Knowledge.ps1, not throwaway state -- keep it tracked despite *.json above.
!asa_claude_package/*.json
asa_claude_package/.knowledge-index.cache.json
